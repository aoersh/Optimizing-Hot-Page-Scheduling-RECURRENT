# 2026-09-10 工作总结：阶段 3 最低可训练版本完成

## 原始工件持久化

将仍位于 `/tmp` 的 W21/90:10、W23 两个 ratio、W27 两个 ratio 共五个正式扫描目录复制到
`/home/hjy/projects/HotPageScheduling-results/`。逐目录比较文件数量和总字节数，源与目标
完全一致；每个目录均包含 65 个 `migration.json`。

从持久目录重新生成 30 条最低训练记录。六个 workload/ratio 场景各 5 条，记录键 30/30
唯一，schema 校验通过；150 个 `source_artifacts` 引用全部存在，且不再包含 `/tmp` 路径。

## socket1 最小对称性验证

将在线 pilot 的 MLC 线程范围、CPU 绑定、CPU NUMA 节点、DRAM/CXL 节点和热访问节点参数化，
默认值保持 socket0 行为不变。socket1 使用 CPU `16-31`、MLC 线程 `17-31`、Node 3→1。

单次 smoke 获得 34383 个 PEBS 样本，Node 1 热候选达到 4096 页，候选页迁移前有 2834 页
位于 Node 3；最终完成 64/64 页 Node 3→1 迁移，迁移错误为 0，MLC 曲线完整 19 点。

随后完成零迁移与 64 页迁移各 5 次配对。迁移平均带宽增加 `92.14 MB/s`，但只有 3/5 次
改善，且平均延迟退化 `1.656 ns`，未通过稳定性门槛。因此 socket1 链路验证成功，但不能
把 socket0 策略直接视为 socket1 的有效标签；这 10 次验证数据不并入当前训练集。

## 阶段结论

阶段 3 的最低可训练版本完成：socket0 六个正式场景均完成真实 PEBS、真实页面迁移、MLC
性能评价和五次配对重复；已得到 10 条正样本与 20 条零迁移负样本，原始工件可追溯并已
持久化。当前训练数据的适用范围明确限定为 socket0 Node 2→0。

完整双 socket 正式矩阵尚未执行，作为后续数据增强项，不阻塞首次训练。下一步进入阶段 4，
实现按 workload/ratio 场景分组的数据切分、MLP 训练、验证和模型工件保存。
