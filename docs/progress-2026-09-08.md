# 2026-09-08 工作总结

## W23/90:10 断点续跑

上次服务器断连后，使用 `RESUME=1` 从首个不完整运行继续扫描。已完成阈值 `{1,2}`、迁移量
`{256,512,1024}`（阈值 1）及 `{32,64,128}`（阈值 2）、间隔 `{0,20}` ms，加零迁移对照，
共 13 组、每组 5 次重复，最终 65/65 次有效运行。

完整性检查全部通过：每次运行均为 `migration_status=0`、`migration_errors=0`、实际迁移数等于
配置上限，并包含完整 19 点 MLC 曲线。perf 仍记录约 2--2.8 万条 out-of-order events/次；
当前热度聚合使用完整窗口计数，不依赖事件顺序，因此该项作为已知测量限制记录。

## 结果与决策

在平均延迟退化不超过 1.0 ns 或 0.5 ns、至少 5 次配对重复、带宽至少 4/5 次改善的规则下，
两套容差均得到 0 个稳定候选，最终决策为 `fallback_zero_migration`。非零迁移组相对零迁移
对照的平均带宽均下降；最佳方向也未达到 4/5 改善门槛。因此该场景作为有效零迁移负样本，
不是实验失败。

汇总和赢家文件保存在 `/tmp/hotpage-perf-check/phase3-mlc-W23-9010-r5/`，原始 perf/MLC
工件不提交仓库。

## W23/75:25 容量探测与正式扫描

容量探测分别请求 4096 页，阈值 1 实际可迁移 2617 页，阈值 2 实际可迁移 385 页；迁移
状态和 MLC 曲线均正常。为给重复间候选数量波动留出余量，正式范围设为阈值 1 的
`{512,1024,2048}` 页、阈值 2 的 `{64,128,256}` 页，间隔均为 `{0,20}` ms。
CXL 配比为 75:25。

正式扫描共 13 组、每组 5 次重复，完成 65/65 次有效运行。所有运行均为
`migration_status=0`、`migration_errors=0`，实际迁移数等于配置上限，且包含完整 19 点
MLC 曲线。阈值 2、256 页、0 ms 的平均带宽增加 54.22 MB/s，但只在 3/5 次配对中改善，
未达到至少 4/5 次同向改善的稳定性门槛。其余配置也没有通过稳定性规则。

因此 1.0 ns 和 0.5 ns 延迟容差下稳定候选数均为 0，W23/75:25 的正式决策同样为
`fallback_zero_migration`。结果位于
`/tmp/hotpage-perf-check/phase3-mlc-W23-7525-r5/`，不提交原始工件。

## 当前项目进展

阶段 0、1、2 已完成；阶段 3 已完成 W21 和 W23 的两个 ratio，尚需 W27 两个 ratio，
以及 socket1 对称拓扑验证。阶段 4（合并训练集和 MLP 训练）尚未开始。

下一步先运行 W27/90:10 容量探测和正式扫描；若出现迁移短缺、MLC 曲线不完整或权限错误，
按约定停止并报告。
