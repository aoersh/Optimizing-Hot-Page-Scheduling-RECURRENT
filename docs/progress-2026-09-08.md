# 2026-09-08 工作总结

## W23/90:10 断点续跑

上次服务器断连后，使用 `RESUME=1` 从首个不完整运行继续扫描。已完成阈值 `{1,2}`、迁移量
`{256,512,1024}`（阈值 1）及 `{32,64,128}`（阈值 2）、间隔 `{0,20}` ms，加零迁移对照，
共 13 组、每组 5 次重复，最终 65/65 次有效运行。

完整性检查全部通过：每次运行均为 `migration_status=0`、`migration_errors=0`、实际迁移数等于
配置上限，并包含完整 19 点 MLC 曲线。perf 仍记录约 2--2.8 万条 out-of-order events/次；
当前热度聚合使用完整窗口计数，不依赖事件顺序，因此该项作为已知测量限制记录。

## 结果与决策

在平均延迟退化不超过 1.0 ns 或 0.5 ns、至少 5 次配对重复、带宽至少 4/5 次改善的规则下，
两套容差均得到 0 个稳定候选，最终决策为 `fallback_zero_migration`。非零迁移组相对零迁移
对照的平均带宽均下降；最佳方向也未达到 4/5 改善门槛。因此该场景作为有效零迁移负样本，
不是实验失败。

汇总和赢家文件保存在 `/tmp/hotpage-perf-check/phase3-mlc-W23-9010-r5/`，原始 perf/MLC
工件不提交仓库。

## 当前项目进展

阶段 0、1、2 已完成；阶段 3 已完成 W21 两个 ratio 及 W23/90:10，尚需 W23/75:25、W27
两个 ratio，以及 socket1 对称拓扑验证。阶段 4（合并训练集和 MLP 训练）尚未开始。

下一步先运行 W23/75:25 容量探测和正式扫描；若出现迁移短缺、MLC 曲线不完整或权限错误，
按约定停止并报告。
