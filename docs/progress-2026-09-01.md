# 2026-09-01 实验进展

## 今日完成

1. 实现阶段 3 稳定标签规则：每个参数组与同 repeat 的零迁移对照配对；至少 5 个重复；
   平均延迟退化不超过 1 ns；带宽改善方向至少 4/5 一致；实际迁移数必须达到配置值；
   没有配置通过时显式回退零迁移。
2. 为 MLC 在线扫描记录 repeat、参数、perf 乱序事件和迁移 shortfall，汇总器可输出全部
   配对效应、稳定候选及 winner 决策。
3. 完成 W21、75:25、socket0/Node 0--2 细扫：阈值 `{1,2}`、迁移量
   `{384,512,768}`、间隔 `{0,20}` ms 加零迁移对照，5 次重复共 65/65 次。
4. 65 次运行均包含 19 点 MLC 曲线，迁移工具错误为 0。阈值 2、768 页两组只能实际
   迁移 520--593 页，因 shortfall 排除。
5. 1.0 ns 和 0.5 ns 两种延迟容差均选择阈值 1、768 页、0 ms：5/5 次带宽改善，
   平均增加 578.54 MB/s；平均延迟改善 0.048 ns，按基本持平解释。
6. 新增 MLC winner 训练数据构建器，生成 W21 75:25 首批 5 条候选记录，全部通过 schema。
7. 自动测试由 7 项增加到 9 项，覆盖稳定配对选择和扫描目录中同名日志文件处理。

## 当前结论

W21 75:25 场景已经具备可靠候选标签。现有结果支持“在延迟不显著恶化的约束下提高带宽”，
不支持“迁移显著降低延迟”。单场景的 5 条记录不能用于正式 MLP 训练，仍需覆盖其他
workload、DRAM/CXL 比例和 socket 拓扑，并按场景划分训练与验证数据。

## 未完成与下次入口

W21 90:10 扫描在结束今日工作前仅完成 1/65 次。该数据不平衡，不参与汇总或训练；原始
文件位于 `/tmp/hotpage-perf-check/phase3-mlc-W21-9010-r5`。当前无残留 MLC、perf 或扫描
进程。下次从 W21 90:10 完整矩阵开始，随后依次运行 W23/W27 和 socket1 对称性验证。

完整 W21 75:25 原始数据位于 `/tmp/hotpage-perf-check/phase3-mlc-fine-r5`，仓库只保存
可重建脚本、配置、规则和结果摘要，不提交 perf/MLC 大文件。
