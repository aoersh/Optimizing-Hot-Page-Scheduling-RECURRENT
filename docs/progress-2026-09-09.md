# 2026-09-09 工作总结

## W27/90:10

完成容量探测和 65/65 次正式扫描。阈值 1、2 的候选容量分别约为 1996、206 页；正式范围
为阈值 1 的 `{384,768,1536}` 页、阈值 2 的 `{32,64,128}` 页，间隔 `{0,20}` ms。
所有运行均迁移状态正常、无错误、实际迁移数匹配、MLC 曲线完整 19 点。两套延迟容差下
均无稳定候选，决策为 `fallback_zero_migration`。

## W27/75:25

完成容量探测和 65/65 次正式扫描。阈值 1、2 的候选容量分别约为 1814、586 页；正式范围
为阈值 1 的 `{384,768,1536}` 页、阈值 2 的 `{64,128,256}` 页，间隔 `{0,20}` ms。
所有运行均通过完整性检查。两套延迟容差下均选出同一稳定赢家：阈值 2、64 页、20 ms，
5/5 次带宽改善，平均增加 `16.82 MB/s`，平均延迟退化 `0.32 ns`，决策为 `migrate`。

## W21/75:25 持久化重跑

由于原 `/tmp` 正式数据缺失，在持久目录
`/home/hjy/projects/HotPageScheduling-results/` 重跑 W21/75:25。阈值 2、768 页在首轮
出现 `574/768` 候选短缺，按严格规则排除该配置；其余 11 组共 55/55 次有效运行完成。
清理诊断目录后，正式赢家为阈值 1、768 页、20 ms：5/5 次带宽改善，平均增加
`622.76 MB/s`，平均延迟改善 `0.624 ns`，两套容差均决策为 `migrate`。

## 最低可训练数据

扩展 `analysis/build_mlc_phase3_dataset.py`，支持从 `fallback_zero_migration` 的零迁移控制组
生成真实负样本，并增加状态、错误数和 19 点曲线完整性过滤。11 个测试全部通过。
当前在持久目录生成 30 条有效记录：W21/75:25 与 W27/75:25 各 5 条正样本，其他四个
场景各 5 条零迁移负样本；schema 和记录键唯一性均通过。

## 未完成事项

阶段 3 的 socket0 论文对照场景已完成，阶段 4 尚未开始。当前 30 条记录中，除 W21/75:25
和 W27/75:25 外，部分 `source_artifacts` 仍指向 `/tmp`，需要先复制到持久目录并更新路径，
再进行正式 MLP 训练。socket1 对称拓扑验证也尚未开始。
