# 2026-09-02 实验进展

## 今日完成

1. 删除与本项目主线无关的 GPU/CXL benchmark、文档和 Makefile 目标，继续阶段 3。
2. 为 MLC 扫描器增加严格断点续跑：只跳过配置字段匹配、`migration_status=0`、
   `mlc_points=19` 且无迁移错误的结果；中断文件原位重跑。
3. 增加 `STRICT_MIGRATION_COUNT=1`，实际迁移数未达到配置值时立即停止，避免配置标签与
   真实动作不一致；增加 `SKIP_CONFIGS` 支持排除已知无效组合。
4. W21 90:10 的初始 `{384,512,768}` 页范围出现系统性候选不足，实际通常只有
   288--363 个合格 Node 2 页面。`{128,256,320}` 校准中 320 页仍出现 314/320 shortfall。
5. 将正式范围冻结为 `{128,192,256}` 页，并完成阈值 `{1,2}`、间隔 `{0,20}` ms、
   一个零迁移对照、5 次重复的 65/65 次扫描。

## W21 90:10 结果

- 65 次运行全部输出 19 个 MLC 点；迁移状态为 0；迁移错误和 shortfall 均为 0。
- 零迁移对照：延迟 87.930 ns，带宽 94989.52 MB/s。
- 所有 12 个非零配置的平均带宽都低于对照，差值为 -27.52 到 -96.16 MB/s。
- 非零配置平均延迟相对对照恶化 0.032--1.018 ns。
- 在 1.0 ns 和 0.5 ns 延迟容差下，稳定候选数都为 0。
- 标签决策：`fallback_zero_migration`。

这是一条有效负标签：W21 90:10 下迁移真实 PEBS 热页没有稳定收益。它与 W21 75:25
选择阈值 1、768 页、0 ms 的正标签共同证明模型必须按场景输出参数，并允许不迁移。

## 原始数据与下一步

正式数据位于 `/tmp/hotpage-perf-check/phase3-mlc-W21-9010-final-r5`。早期 shortfall 数据
保留在同级 `phase3-mlc-W21-9010-r5` 和 `phase3-mlc-W21-9010-small-r5`，仅用于范围校准，
不进入训练集。下一步按相同稳定性规则运行 W23 的 90:10 和 75:25 场景。
