# CXL 热页调度 MLP 预测实验复现项目规划书

## 1. 文档信息

- 复现对象：*Optimizing Hot Page Scheduling in CXL Memory via Multilayer Perceptron-Based Prediction*
- 论文出处：IEEE ISPA 2025，DOI `10.1109/ISPA67752.2025.00037`
- 规划日期：2026-08-21
- 当前阶段：阶段 0 已完成；阶段 1 pilot 基线已跑通；阶段 2 真实 PEBS 热度驱动迁移闭环已完成；下一步进入阶段 3 参数扫描与训练集生成
- 总体目标：构建一个可执行、可审计、可重复的双层内存热页迁移系统，复现论文的 MLP 参数预测闭环，并在可比实验中验证动态策略相对静态策略的延迟与带宽收益。

## 2. 复现范围与成功定义

### 2.1 核心复现范围

1. 在真实四 NUMA 节点上建立 DRAM/CXL 分层：Node 0/1 为 DRAM，Node 2/3 为 CXL；以 0/2 和 1/3 作为论文二节点对照。
2. 运行 Intel MLC 的 W21、W23、W27 工作负载。
3. 从运行时状态提取论文规定的 6 个特征。
4. 通过参数扫描生成最优迁移参数标签和训练集。
5. 训练 `6 -> 32 -> 16 -> 3` 的 MLP 回归模型。
6. 在线预测最大迁移页数、访问差阈值和迁移周期，并执行页面迁移。
7. 比较无迁移、静态迁移、动态 MLP 迁移三种策略。
8. 覆盖 90:10 和 75:25 两种 DRAM:CXL 容量配比，输出带宽、延迟及迁移开销结果。

### 2.2 分层成功标准

- L1，功能复现：完整闭环可运行，页面确实跨节点迁移，日志和数据可追踪。
- L2，方法复现：模型、特征、工作负载、容量比例、基线和论文一致，结果方向一致。
- L3，数值复现：动态策略接近论文报告均值；考虑硬件和虚拟化差异，不把逐点完全相同作为硬性要求。
- L4，增强复现：补充重复次数、置信区间、模型误差、迁移成本和消融实验，使结论可审计。

## 3. 论文规格台账

### 3.1 论文明确给出的内容

| 类别 | 论文规格 |
|---|---|
| 主机 | Intel Core i7-11700；正文称 4 cores |
| 系统 | Ubuntu 22.04.5 LTS，Linux 6.8 |
| 内存架构 | QEMU 双 NUMA 节点；Node 0 模拟本地 DRAM，Node 1 模拟 CXL memory |
| 工作负载 | Intel MLC：W21（100% 顺序读）、W23（3:1 读写）、W27（2 次读后 1 次 non-temporal write） |
| 容量比例 | DRAM:CXL = 90:10、75:25 |
| 输入特征 | `total_pages`、`avg_n0`、`avg_n1`、`max_delta`、`high_delta_pages`、`imbalance_ratio` |
| 模型输出 | `MAX_MIGRATIONS`、`ACCESS_DIFF_THRESHOLD`、`MIGRATION_INTERVAL` |
| 网络 | 全连接 `6 -> 32 -> 16 -> 3`，隐藏层 ReLU，输出层线性 |
| 初始化 | He normal；bias 为 0 |
| 预处理 | 输入特征 Z-score 标准化，保存 StandardScaler |
| 损失与优化 | MSE，Adam，初始学习率 `1e-3` |
| 训练 | 200 epochs；全数据集 batch；样本数 >= 10 时按 8:2 划分；每 20 epochs 计算验证损失 |
| 模型文件 | `parampredictor.pt`，并保存配套 scaler |
| 静态阈值实验 | `ACCESS_DIFF_THRESHOLD` = 10、20、30 |
| 论文脚本名 | `collect_global_features_ext.py`、`train_param_model.py`、`predict_params.py`、`mlc_minimal_patched.sh` |
| 观测入口 | 论文称监视 `/proc/PID/numa_maps` |
| 主要指标 | MLC latency（ns）、bandwidth（MB/s） |

### 3.2 论文报告的结果锚点

| DRAM:CXL | 策略 | 平均延迟 | 平均带宽 |
|---|---|---:|---:|
| 90:10 | 静态 | 65.98 ns | 970.13 MB/s |
| 90:10 | 动态 | 60.81 ns | 985.93 MB/s |
| 75:25 | 静态 | 66.37 ns | 964.47 MB/s |
| 75:25 | 动态 | 62.49 ns | 983.03 MB/s |

论文另称 W23-90:10 最大延迟下降 6.31 ns（9.5%），W23-75:25 最大带宽增加 24.2 MB/s（2.5%）。摘要声称最高带宽提升 22.48%、延迟降低 19.29%，但正文图表和均值不足以直接推出这两个峰值，复现时分别记录，不能混为同一比较口径。

### 3.3 未充分披露或存在歧义的内容

以下信息无法仅从论文唯一确定，实施前需通过预实验固定并写入配置：

- QEMU 版本、完整启动参数、vCPU/内存总量、NUMA distance 和远端内存延迟/带宽如何模拟。
- 论文所称 i7-11700 “4 cores”与该 CPU 标称核心数不一致。
- MLC 版本、W21/W23/W27 的实际配置文件、线程数、工作集、预热及测量时长。
- `/proc/PID/numa_maps`本身主要提供映射和驻留页统计，不能直接给出逐页访问次数；论文未说明访问计数来源。
- 三个输出参数的扫描范围、步长、单位、下界 safeguard 具体数值。
- “最优”标签的目标函数：延迟、带宽，或二者的加权组合。
- 训练样本总量、随机种子、采样时间窗、重复次数、模型输出是否缩放/取整/截断。
- 实际迁移工具及权限机制，可能是 `move_pages(2)`、`migrate_pages(2)` 或自定义程序。
- 图 12 至图 15 的文字标题与横轴比例标签疑似互换，应以横轴标签和正文数值为准。

## 4. 当前环境评估与目标架构

本节记录 2026-08-21 在目标服务器上的实测环境；后续每次实验须将同类信息写入 run manifest。

### 4.1 实测硬件与软件

| 项目 | 实测值 | 对复现的影响 |
|---|---|---|
| CPU | 2 x Intel Xeon 6515P，32 核/64 线程，AVX-512/AMX 可用 | 与论文 i7-11700/QEMU 不等价；固定 CPU 集合和频率策略 |
| 内核/发行版 | Ubuntu 22.04.5，Linux 6.8.0-136-generic | 与论文软件大体接近 |
| NUMA Node 0/1 | 各约 64 GiB，分别绑定 socket 0/1 CPU | 本地 DRAM 基准域 |
| NUMA Node 2/3 | 各 64 GiB、无 CPU、memory-only | CXL 内存已被 `dax_cxl`/`kmem` 映射为可迁移 NUMA 内存 |
| NUMA distance | 0-2=14、1-3=14；0-3=24、1-2=24 | CXL 有明确近端/远端差异，不能合并为单一“远端”节点 |
| CXL | `mem0` PCI `0000:b4:00.0`、`mem1` PCI `0000:33:00.0`；各 64 GiB | 两个真实 CXL Type-3 设备，可直接做 Node 2/3 对照 |
| CXL 观测 | `cxl_pmu_mem0.0`、`cxl_pmu_mem1.0`，含读写请求事件 | 增加设备级流量指标，辅助验证页采样 |
| 工具/限制 | `numactl`、`perf`、`cxl`、`ndctl`、`daxctl` 已安装；Intel MLC v3.13 已本地安装；当前 `perf_event_paranoid=0` | PEBS load 采样、CXL PMU 和 MLC 已验证；DAMON sysfs 仍未暴露 |
| 内核策略 | `numa_balancing=1`，`zone_reclaim_mode=0`，demotion 未启用 | 主实验先保持现状并记录；另做关闭自动 NUMA balancing 对照 |

### 4.2 因地制宜的实验架构

主复现路径改为物理机，不再把 QEMU 作为前置条件：

```text
CPU/socket 0 (Node 0 DRAM) ---- CXL near (Node 2, mem1/dax0.0)
CPU/socket 1 (Node 1 DRAM) ---- CXL near (Node 3, mem0/dax9.0)
             \\---- cross-socket CXL：0->3、1->2（高 distance）
```

每个进程固定在一个 socket 的 CPU 上，使用 `mbind`/`numactl --membind` 将初始页分配到对应 DRAM 或 CXL 节点；迁移只在明确的源/目标节点对之间执行。论文二节点结果只作为“忠实对照”：Node 0 -> Node 2（或 Node 1 -> Node 3），不能与四节点结果混报。

正式场景分为三层：

1. **论文对照层**：单 socket、Node 0/2 或 Node 1/3，容量按 90:10、75:25 配置。
2. **双 CXL 层**：Node 0/1 DRAM 与 Node 2/3 CXL，分别测试近端 CXL 和跨 socket CXL。
3. **四节点增强层**：按热度将页在 Node 0、1、2、3 间迁移，评估 NUMA distance 感知的调度。

`total_pages` 等六个输入特征改为按“源节点、目标节点、窗口”计算；`avg_n0/avg_n1` 在忠实层保留论文命名，在增强层扩展为四节点向量 `avg_n[0..3]`，并训练独立的四节点模型，避免输入维度混用。

推荐闭环：

```text
MLC workload
    -> page/access sampler
    -> 6D feature collector
    -> scaler + MLP inference
    -> parameter validation/rounding
    -> hot-page selector
    -> move_pages migration executor
    -> MLC metrics + migration telemetry
    -> CSV/JSONL artifact store
```

## 5. 拟建项目结构

```text
HotPageScheduling/
├── README.md
├── PROJECT_PLAN.md
├── configs/              # 环境、扫描空间、工作负载及实验矩阵
├── docs/                 # 方法说明、环境记录、复现差异和结果报告
├── env/                  # 物理拓扑快照、权限和依赖检查
├── src/
│   ├── collector/        # 页驻留与访问热度采样、特征计算
│   ├── migration/        # 候选页选择和 move_pages 执行器
│   ├── model/            # 数据集、MLP 训练、评估和推理
│   └── controller/       # 在线闭环控制器
├── scripts/              # 数据采集、参数扫描、实验编排
├── tests/                # 单元、集成和最小烟雾测试
├── data/                 # 原始/处理中间数据；大文件默认不入库
├── models/               # 权重、scaler、训练元数据
└── results/              # 每次运行的配置快照、日志、指标和图表
```

## 6. 数据与接口设计

### 6.1 原始采样记录

每个采样窗口至少记录：实验 ID、时间戳、PID、虚拟地址或脱敏页 ID、当前 NUMA 节点、Node 0/1 访问计数、页大小、采样来源。若内核无法提供可靠逐页访问计数，则优先评估 DAMON、`perf mem`/PEBS 或 PTE accessed-bit sampling，并单独记录方法误差与开销。

### 6.2 特征语义

- `total_pages`：目标进程纳入监测的总页数。
- `avg_n0`、`avg_n1`：窗口内各页对 Node 0/1 的平均访问量。
- `max_delta`：`max(abs(n0_i - n1_i))`。
- `high_delta_pages`：满足 `abs(n0_i - n1_i) >= threshold` 的页数；边界采用 `>=` 还是 `>`须固定。
- `imbalance_ratio`：论文没有公式；候选定义为 `abs(sum_n0-sum_n1)/(sum_n0+sum_n1)`，预实验后冻结。

注意：`high_delta_pages`依赖待预测的 `ACCESS_DIFF_THRESHOLD`，可能形成循环依赖。忠实复现层可使用当前/上一周期阈值；增强层将对固定参考阈值、上一周期阈值分别消融。

### 6.3 训练样本与标签

每行包含 6 个特征、3 个最优参数、工作负载、内存比例、重复编号、候选配置、带宽、延迟、迁移页数、迁移耗时及失败数。原始测量与聚合数据分开保存，避免只保留“最优值”而无法复核。

### 6.4 模型输出约束

MLP 连续输出必须经过显式后处理：反缩放（若输出也缩放）、有限值检查、四舍五入、下界/上界裁剪及单位转换。每次推理同时记录原始输出和生效参数，避免 silent clamp。

## 7. 实施阶段与里程碑

### 阶段 0：环境固化与权限验证

- 将已确认的物理 CXL 四节点拓扑生成机器可读快照，并记录 CXL region、DAX target node 和 PMU 事件。
- 验证 `move_pages(2)`、DAMON/perf、CXL PMU、CPU governor 和调整 NUMA balancing 所需权限。
- 确认 MLC 与 PyTorch 是否可用；MLC v3.13 已安装，下一步冻结 W21/W23/W27 per-thread 配置。
- 冻结“论文忠实层”和“增强层”的差异清单。
- 交付：环境清单、依赖检查脚本、可复现实验配置模板。

验收：一条命令生成机器可读环境报告；所有缺失依赖和权限均有明确状态。

当前状态：已实现并运行 `scripts/run_phase0.sh`。Node 0 -> 2 与 Node 1 -> 3
各迁移 4096 个 4 KiB 页面，迁移前后驻留查询均 100% 符合预期且无错误；
CXL PMU、PEBS 和 MLC v3.13 已安装并通过 smoke test；实验期间将 `perf_event_paranoid` 临时调整为 0，DAMON sysfs 仍未暴露。

### 阶段 1：物理 DRAM/CXL 基线

- 首选 socket 0 的物理核 `0-15`，对照节点为 DRAM 0/CXL 2；用 socket 1 的物理核 `16-31` 和 DRAM 1/CXL 3 做重复性验证，默认不启用 SMT sibling。
- 通过工作集页面的初始分配量实现 90:10、75:25 DRAM:CXL 有效比例，并用 `move_pages` 查询结果验收；不修改整机节点容量。
- 分别测量 0/2、1/3、0/3、1/2 的空载延迟和带宽，建立近端/跨 socket 基线。
- 固化 MLC W21/W23/W27 配置、工作集、线程、预热和运行时长。
- 收集无迁移基线并验证重复运行稳定性。

验收：初始页驻留比例误差 <= 1%；目标节点间可双向迁移；每个场景至少 5 次重复，变异系数目标 <= 5%，否则先定位噪声。

当前状态：已用自研 `memory_bench` 完成 6 个 CPU/内存节点组合的 3 次 pilot，
页面驻留校验全部通过。结果见 `docs/phase1-baseline.md`；该数据用于确认闭环和数量级，
不替代论文 MLC 结果。

### 阶段 2：热度采样与迁移执行器

- 验证论文所述 `/proc/PID/numa_maps` 能得到哪些数据。
- 选择并实现逐页热度数据源；做采样开销对照。
- 使用 `move_pages(2)` 查询驻留节点、批量迁移并检查返回状态。
- 实现静态阈值策略和限速/周期控制。

验收：已知分配模式下页驻留识别正确；迁移前后节点变化可验证；部分失败不会破坏整轮实验；所有 errno 和耗时有日志。

当前状态：迁移执行器 pilot 已完成，覆盖四组 DRAM/CXL 拓扑和 90:10、75:25
混合驻留，逐页迁移错误为 0，结果见 `docs/phase2-migration.md`。逐页热度采样、
静态阈值策略已用软件访问计数器跑通阈值 10/20/30，结果见
`docs/phase2-static-controller.md`。在线周期控制 pilot 已实现并验证周期、限速和迁移后驻留，结果见
`docs/phase2-online-controller.md`。PEBS 真实逐页热度采样与六维特征汇总已实现，能够从
MLC 工作集生成页级 JSONL，但当前单 CPU MLC 采样只能观测一个 CPU NUMA 节点，且
64 MiB 工作集的 x20 pilot 页面覆盖率为 26.59%。正式特征仍需
多节点工作负载和更高采样覆盖率。
双节点受控热度 pilot 已进一步验证 CPU 0/16 对同一 8 MiB 工作集的相反 80:20 偏好：
PEBS 覆盖 97.07% 页面，前后半区分别恢复出 3.03:1 和 3.65:1 的正确访问方向，结果见
`docs/phase2-pebs-dual-node-validation.md`；采集与特征格式见
`docs/phase2-pebs-page-heat.md`。
采样周期扫描已完成：当前 pilot 默认 `SAMPLE_PERIOD=1000`，三次重复页面覆盖率均值
95.70%，已判定页面准确率 72.93%；该数据用于热度排序，不作为无噪声逐页标签。
真实 PEBS 热度驱动跨进程迁移闭环已完成：4 MiB CXL Node 2 工作集采样 7265 个有效样本，
Node 0/1 各迁移 64 页，迁移后逐页复核错误为 0，结果见
`docs/phase2-real-heat-migration.md`。阶段 2 工程验收完成。

下一步已进入参数敏感性 pilot：扫描阈值 `{10,20,30}` 与 `MAX_MIGRATIONS`
`{128,256,512}`，结果写入 `docs/phase2-controller-sweep.md`。

阶段 2 已确认的问题、废弃数据原因、测量口径和解决条件统一维护在
`docs/phase2-known-issues.md`，正式结果必须满足其中的放行标准。

### 阶段 3：参数扫描与训练集生成

- 先用论文明确的阈值 `{10,20,30}` 复刻阈值敏感性实验。
- 通过小规模 pilot 确定 `MAX_MIGRATIONS` 和 `MIGRATION_INTERVAL` 的合理范围与单位。
- 对每个 workload x ratio x 参数组合做重复测量，随机化运行顺序。
- 固定标签目标函数；建议主目标优先最小延迟，在相近延迟下最大带宽，并报告替代加权目标的敏感性。

验收：训练数据有 schema 校验、无场景泄漏、每个标签可回溯到原始运行记录；扫描空间与选优规则写入版本化配置。

### 阶段 4：MLP 训练与离线评估

- 实现论文规定的模型、初始化、标准化、MSE、Adam 和 200 epochs。
- 固定随机种子并保存权重、scaler、配置、数据 hash、训练曲线。
- 除论文 8:2 划分外，增加按场景分组的验证，防止同场景重复样本泄漏。
- 报告各输出 MAE/RMSE、裁剪率，并比较常数/均值预测基线。

验收：同一数据和种子可重现；checkpoint 可独立加载推理；模型在留出场景上优于简单基线，或如实报告未优于。

### 阶段 5：在线闭环与完整实验

- 串联采样、特征、预测、候选页选择、迁移和记录。
- 加入预热期、推理失败回退静态参数、最大迁移速率及干净退出。
- 对无迁移、静态阈值、MLP 动态三种策略运行完整矩阵。
- 每个条件建议至少 10 次独立重复，运行顺序随机化。

验收：每轮可由 run manifest 完整重放；进程结束无遗留控制器；动态策略的参数变化与迁移动作可追踪。

### 阶段 6：统计分析与复现报告

- 输出均值、中位数、标准差、95% CI、相对改善和效应量。
- 绘制论文图 9 至图 15 的对应图，并标注论文值与复现值。
- 分解采样、推理、迁移开销；补充模型消融和阈值敏感性。
- 对每项偏差归因：真实 CXL/论文模拟环境、NUMA distance、热度来源、参数空间或随机误差。

验收：所有图表可由原始结果一键生成；结论不只依赖最佳单次运行；正文与图轴比例保持一致。

## 8. 实验矩阵

主矩阵预计为：

- 工作负载：W21、W23、W27，共 3 类。
- 容量比：90:10、75:25，共 2 类（按工作集页面的初始驻留比例实现；不离线下线 CXL 内存）。
- 拓扑组：首选论文对照 `0<->2`；socket 对称验证 `1<->3`；跨 socket CXL `0<->3`/`1<->2`；四节点增强，共 4 类。
- 策略：无迁移、静态、动态 MLP，共 3 类。
- 重复：正式实验建议 10 次。
- 主矩阵运行数：`3 x 2 x 4 x 3 x 10 = 720`，不含预热、参数扫描和失败重跑；资源不足时先完成论文对照层。

附加实验：

- 静态阈值 10/20/30 的延迟和带宽敏感性。
- MLP 与均值预测、线性回归等简单基线比较。
- 去除单个输入特征的消融。
- 控制周期和最大迁移页数的敏感性。
- 采样开启/关闭、迁移开启/关闭的独立开销。
- CXL PMU 读写请求与页级热度的一致性验证。
- 近端 CXL 与跨 socket CXL 的 distance-aware 调度消融。
- 训练场景内验证与未见 workload/ratio 的外推验证。

## 9. 指标、统计与结果判定

### 9.1 主指标

- MLC 平均/分位延迟（ns）。
- MLC 带宽（明确统一 MB/s 或 MiB/s）。
- 相对改善：延迟按 `(baseline-dynamic)/baseline`，带宽按 `(dynamic-baseline)/baseline`。

### 9.2 系统与模型指标

- 尝试迁移、成功迁移、失败迁移页数及字节数。
- 每轮迁移耗时、控制周期抖动、采样和推理耗时。
- 节点驻留分布、回迁/抖动次数。
- 三个输出各自 MAE/RMSE、输出裁剪率、验证损失。

### 9.3 判定规则

- 先判断实验有效性和统计不确定性，再判断是否复现收益方向。
- 不以“最高提升”替代全矩阵结果，也不只挑选有利工作负载。
- 数值若未达到论文值，闭环功能正确且差异能由环境或未披露参数解释，仍可判为方法复现成功、数值复现未完成。

## 10. 风险与缓解措施

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| MLC 获取或许可限制 | 无法使用原 benchmark | 优先使用官方 MLC；保留 STREAM/自研微基准仅作工程验证，不冒充论文主结果 |
| CXL CLI 版本较旧且无法完整解析 sysfs | 设备健康/region 元数据不完整 | 以 sysfs、PCI、DAX target node 交叉验证；需要时升级 ndctl/cxl-cli |
| `perf_event_paranoid=4` 或权限不足 | PEBS/CXL PMU 无法采样 | 阶段 0 做最小权限测试；申请 CAP_PERFMON/root，仅对实验命令授权 |
| `numa_maps`无法提供访问频次 | 6 个特征无法忠实计算 | 评估 DAMON、PEBS/perf mem、PTE accessed bit；记录与论文的偏差 |
| 自动 NUMA balancing 干扰 | 页面被内核额外迁移 | 固定并记录设置；分别做关闭/开启对照，主实验保持一致 |
| THP/大页影响页粒度 | 热度和迁移统计失真 | 固定 THP 策略并记录页大小；必要时关闭 THP 做主实验 |
| 参数扫描组合爆炸 | 时间成本过高 | pilot 缩小范围，再分阶段粗搜/细搜；保留完整搜索日志 |
| 训练数据泄漏 | 模型结果虚高 | 按 workload/ratio/run 分组切分，重复样本不跨集合 |
| MLP 输出不可执行 | 负数、超范围或抖动 | 明确取整、裁剪、下界、回退策略，并记录 raw/effective output |
| 论文结果口径矛盾 | 无法判断数值一致性 | 建立结果锚点表，图、正文和摘要分别核对并披露差异 |
| 主机负载噪声 | 重复性不足 | CPU pinning、固定频率策略、预热、随机化顺序、记录背景负载 |

## 11. 可复现性要求

- 所有环境、模型、扫描范围和工作负载参数均放入版本化配置，禁止只写在命令历史中。
- 每次运行生成唯一 ID 和 manifest，包含 git revision、配置 hash、物理主机/CXL 拓扑、随机种子和时间。
- 原始日志不可被聚合脚本覆盖；处理结果保存输入 hash。
- 固定单位、字段 schema、缺失值规则和异常运行判据。
- 脚本失败时返回非零状态；实验编排区分失败、跳过和有效完成。
- 结果报告同时列出论文已知信息、我们的假设和实际环境差异。

## 12. 预计交付物

1. 四节点物理 DRAM/CXL 拓扑快照、权限检查和基线报告。
2. 可验证的热度采样器、特征收集器和页面迁移执行器。
3. 静态迁移基线及阈值敏感性数据。
4. 参数扫描数据集、MLP 训练/评估/推理程序及模型工件。
5. 在线自适应迁移控制器和一键实验编排入口。
6. 原始结果、统计分析、论文对照图和最终复现报告。
7. 自动测试、操作说明、限制和偏差说明。

## 13. 阶段门与待确认事项

在开始实现前，建议确认以下事项：

物理 CXL 主路径已经确定。进入基准执行前仍需确认：

1. 提供或安装 Intel MLC 可执行文件并接受其许可；MLC 不提交到项目仓库。
2. 提供运行 `perf`/CXL PMU、跨进程 `move_pages`、短时关闭自动 NUMA balancing 所需的最小权限。
3. 对论文未披露参数采用“预实验确定 + 配置固化 + 报告明确披露”。
4. 正式结果将论文对照层和四节点增强层分开报告，不以真实 CXL 数据冒充论文 QEMU 数值复现。

下一步进入阶段 0：编写环境快照与最小 DRAM/CXL 分配、迁移、测量程序；任何全局内核设置变更均在执行前单独确认。
